export interface WaitingPassengerType {
    id: string
    date: string
    time: string
    departurePlace: string
    destination: string
    capacity: string
    memo: string
    userName: string
}