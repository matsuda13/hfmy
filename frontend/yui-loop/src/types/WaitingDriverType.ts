export interface WaitingDriverType {
    id: string
    date: string
    time: string
    departurePlace: string
    destination: string
    memo: string
    userName: string
    gender: string
    grade: string
}